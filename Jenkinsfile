pipeline {
    agent {
        label 'java-builder'
    }

    stages {
        stage('Success') {
            steps {
                echo 'Hello'
                echo 'success'
            }
        }
    }
}
