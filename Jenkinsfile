pipeline {
    agent {
        label 'java_builder'
    }

    stages {
        stage('Success') {
            steps {
                echo 'Hello'
                echo 'success'
            }
        }
    }
}
