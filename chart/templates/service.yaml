apiVersion: v1
kind: Service
metadata:
  name: {{ include "students-app.fullname" . }}
  labels:
    {{- include "students-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "students-app.selectorLabels" . | nindent 4 }}
